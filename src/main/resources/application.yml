server:
  port: 5050
---
environment: production
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/spring-repo2
    username: root
    password: 9876
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    database-platform: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: update
    show-sql: true
  application:
    name: hibernate